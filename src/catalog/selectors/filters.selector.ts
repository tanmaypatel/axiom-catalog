import { Phone } from '../models/phone';
import { ISelectedFilters } from '../models/filters';

const applyFilters = (phones: Phone[], selectedFilters: ISelectedFilters): Phone[] => {
    return [];
};

export { applyFilters };
